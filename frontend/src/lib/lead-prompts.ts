/* Lead sourcing prompts, mirrored from Notes/LEAD_PROMPTS.html.
   Generated: the prompt bodies are the source of truth in that file and are
   copied here verbatim, so edit them there and re-export rather than
   touching the strings below. */

export interface LeadPrompt {
  id: string;
  n: number;
  title: string;
  body: string;
}

export const LEAD_PROMPTS: LeadPrompt[] = [
  {
    "id": "p1",
    "n": 1,
    "title": "E-commerce Brands",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS e-commerce brand owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: Online retail / DTC brands. 11-200 employees. COUNTRIES.\njob_level: owner, founder, president. NOT c-suite — it returns CPO/CRO/CFO\nseats who do not own customer support.\n\nINDUSTRY: naics_category [\"4541\", \"454110\", \"454111\"] (Electronic Shopping and\nMail-Order Houses) — validated. NOT linkedin_category \"retail\", which is full\nof physical stores.\n\nINTENT: none. Validated as too thin — 3 records total.\n\nVERIFY: brands that SELL THEIR OWN PRODUCTS online. Reject agencies,\nmarketplaces, e-commerce SaaS, 3PLs and dropship-course sellers.\n\nEXCLUDE: pass exclude_key \"ds-c02858b7-10ca-42b1-ba9e-f6cc8146809f\" on the\nfetch so I get people I do not already have.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> widen company_size, then drop the least\n  important filter. Export what you have rather than returning empty.\n- Wrong job seats          -> narrow job_level, never widen it\n- Wrong industry           -> switch to the NAICS fallback\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              repeat until it looks right, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve any categories through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to ecommerce_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, and how\nmany rows have a non-empty company_description."
  },
  {
    "id": "p2",
    "n": 2,
    "title": "Dental & Medical Practices",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS dental practice owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: Dental practices. Accept general medical, veterinary and optometry clinics\nif dental volume is thin. 1-50 employees. COUNTRIES.\njob_level: owner, founder, president. Add \"manager\" only if that returns fewer\nthan ROWS — practice manager is an acceptable buyer here. NOT c-suite: it\nreturns CFO/CPO-type seats who do not own the front desk.\n\nINDUSTRY: autocomplete linkedin_category for \"dentist\", then \"dental practice\"\n(one concept per call). If either is broad, use naics_category 6212 / 621210\n(Offices of Dentists) instead.\n\nVERIFY — SCREEN HARD, 20 of 30 failed this on the last run. Dental-tech\nvendors carry NAICS 6212 too, so no filter catches them; only reading the\ndescription does. Keep only rows whose company_description says they TREAT\nPATIENTS. Reject software, labs, agencies, coaches, DPO/partnership networks,\ne-commerce and AI startups. Already known bad: Toothy.ai, Dental Life Coach,\nDentMent, orthobrain, RootRadar, SayCheeseClub, OTC Dental, Nierman,\nAccelerate Dental. Because the reject rate is this high, fetch 3x ROWS here,\nnot 2x — the extra rows are free and you will discard most of them.\n\nINTENT: none. Too thin for small non-tech businesses (3 records on a\ncomparable run). Only if the no-intent pool exceeds 200, try \"ai booking\nagent\".\n\nEXCLUDE: pass exclude_key \"ds-a3bb0569-e28c-41ab-b967-d56cc3c114ec\" on the\nfetch so I do not pay again for the 30 I already have.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> widen company_size, or add \"manager\" to\n  job_level. Export what you have rather than returning empty.\n- Wrong job seats          -> narrow job_level, never widen it\n- Wrong industry           -> switch to the NAICS fallback\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              repeat until it looks right, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve any categories through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 3x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to dental_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, how many\nrows have a non-empty company_description, and how many candidates you\nrejected on the VERIFY rule."
  },
  {
    "id": "p3",
    "n": 3,
    "title": "Home Services",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS home services business owners\nand deliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: HVAC, plumbing, roofing, electrical and landscaping contractors. 1-50\nemployees. COUNTRIES. job_level: owner, founder, president.\nNOT c-suite — these are small firms and the owner decides.\n\nINDUSTRY: autocomplete linkedin_category for \"construction\", then separately\n\"facilities services\" — one concept per call. Both are broad, so expect the\nNAICS fallback to win: naics_category 238220 (Plumbing, Heating,\nAir-Conditioning), 238160 (Roofing), 238210 (Electrical).\n\nVERIFY: contractors that DO THE WORK. Reject construction software vendors,\nlarge commercial builders, building-materials suppliers and franchise\nhead offices.\n\nINTENT: none. Intent data for trades is consistently too thin. Only add it if\nthe no-intent pool exceeds 200.\n\nEXCLUDE: none yet — this vertical has not been run. Report the dataset_id at\nthe end so I can add one here.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> add more NAICS trade codes, then widen\n  company_size. Export what you have rather than returning empty.\n- Wrong job seats          -> narrow job_level, never widen it\n- Wrong industry           -> switch to the NAICS fallback\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              repeat until it looks right, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve any categories through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to home_services_<YYYYMMDD-HHMM>.csv and give me the full path. If a column\n   I care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, and how\nmany rows have a non-empty company_description."
  },
  {
    "id": "p4",
    "n": 4,
    "title": "Small Law Firms",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS small law firm owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: Law firms, 1-50 employees (solo practitioners and small partnerships).\nCOUNTRIES. job_level: owner, founder, partner, c-suite.\nc-suite is correct HERE specifically — at small firms it returns managing\npartners and principals. Validated on a previous run.\n\nINDUSTRY: linkedin_category [\"law practice\"] — validated, 100% NAICS 54111.\nNOT \"legal services\", the loose parent full of legal tech and recruiters.\nnaics_category 541110 is the fallback.\n\nVERIFY: firms that PRACTISE LAW and take clients. Reject legal-tech vendors,\nlegal recruiters, litigation funders and bar associations.\n\nINTENT: keep all four, resolved via autocomplete — \"ai for customer service\",\n\"customer service automation\", \"appointment scheduling software\", \"ai\nautomation\". Expect ~280 prospects.\n\nEXCLUDE: pass exclude_key \"ds-b4ba5ac7-1350-4f34-b326-f16541d460c9\" on the\nfetch so I get people I do not already have.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> drop intent first, keep the category. Export\n  what you have rather than returning empty.\n- Wrong job seats          -> narrow job_level, never widen it\n- Wrong industry           -> switch to NAICS 541110\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              repeat until it looks right, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve any categories and intent topics through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to law_firms_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, and how\nmany rows have a non-empty company_description."
  },
  {
    "id": "p5",
    "n": 5,
    "title": "Real Estate Agencies",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS real estate agency owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\nThe 2026-09-10 run cost 600 credits because it exported a 150-row table with\nfour enrichments and never passed `limit`. Pass `limit`.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: Residential brokerages, real estate agencies and property management\nfirms. 1-200 employees. COUNTRIES.\n\nJOB TARGETING — the filter that actually matters here, learned 2026-09-10.\njob_level: owner, founder. NOT president: in this data \"president\"\noverwhelmingly means divisional presidents and advisory-firm principals\n(\"President of construction\", \"President of Lee Management Services\"), and it\nis what drags investment firms into the results.\n\nThen ALSO filter job_title to broker seats, resolved via autocomplete:\n  [\"broker owner\", \"owner broker\", \"broker / owner\", \"broker-owner\",\n   \"owner / broker\", \"principal broker / owner\", \"managing broker / owner\",\n   \"real estate broker / owner\", \"managing broker\", \"principal broker\",\n   \"broker in charge\", \"realtor\", \"real estate agent\", \"estate agent\",\n   \"real estate broker\"]\nThis is what fixed the run: wrong-buyer rate went 5/5 to 0/5. Note \"realtor\"\nis US vocabulary — if UK/AU/CA come back empty, that is why, and the fix is\nto run those countries with the estate-agent titles only.\n\nINDUSTRY. Use ONLY these narrow linkedin_category values:\n  [\"real estate agents and brokers\", \"leasing residential real estate\",\n   \"leasing real estate agents and brokers\"]\nNOT the broad \"real estate\" or \"commercial real estate\" — those returned\ninvestment banks and JV-equity firms. naics_category 531210 is the fallback.\nDo NOT expect the category alone to fix wrong buyers: tested 2026-09-10, all\nthree narrow categories still gave 5/5 investment firms, and NAICS 531210\nalone still gave 3/5. The job_title filter is what fixes it.\n\nVERIFY: brokerages with LISTINGS AND BUYERS. Reject investment management,\nasset management and capital markets. The prospect_skills field is the\nquickest tell — real brokers show \"Listings\", \"Buyer representation\", \"First\ntime home buyers\", \"Sellers\". If more than a third of the sample is\ninvestment firms, tighten the TITLES and re-sample before enriching.\n\nINTENT: none. The narrow categories WITH intent returned only 5 prospects on\na previous run.\n\nEXCLUDE: pass exclude_key \"ds-7cb22b25-225e-42d5-b5c9-5a87d98a1f2b\" on the\nfetch so I do not pay again for the 46 I already have.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords (every real estate site says \"appraisal\") or\ncompany_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> widen company_size, or add NAICS 531210\n  alongside. Export what you have rather than returning empty.\n- Investment firms present -> tighten the job_title list (drop \"realtor\" and\n  \"real estate agent\", keep the broker/owner titles). Do NOT just narrow the\n  category — that was tried and it does not work\n- Non-US countries empty   -> run them separately with \"estate agent\" titles\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve the categories and every job_title through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to real_estate_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, how many\nrows have a non-empty company_description, and the country split."
  },
  {
    "id": "p6",
    "n": 6,
    "title": "Marketing Agencies",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS marketing agency owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: Marketing, digital, creative and advertising agencies. 11-200 employees.\nCOUNTRIES. job_level: owner, founder, president. Add c-suite only if that\nreturns fewer than ROWS — at agencies the founder is the buyer.\n\nINDUSTRY: autocomplete linkedin_category for \"marketing services\", then\nseparately \"advertising services\", then separately \"design services\" — one\nconcept per call. Include all that are clearly agency-side.\n\nVERIFY: agencies that SERVE CLIENTS. Reject martech SaaS products, ad\nnetworks, in-house marketing teams and freelance-marketplace platforms.\n\nINTENT: try \"ai automation\" and \"ai consulting\" resolved via autocomplete.\nAgencies have better intent data than small local businesses. If the intent\npool is under 30, drop intent and keep the category.\n\nEXCLUDE: none yet — this vertical has not been run. Report the dataset_id at\nthe end so I can add one here.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> drop intent first, then widen company_size.\n  Export what you have rather than returning empty.\n- Wrong job seats          -> narrow job_level, never widen it\n- SaaS products present    -> tighten to services-only categories\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              repeat until it looks right, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve any categories and intent topics through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to marketing_agencies_<YYYYMMDD-HHMM>.csv and give me the full path. If a\n   column I care about lands in its \"unrecognised\" list, tell me — the script\n   needs a new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, and how\nmany rows have a non-empty company_description."
  },
  {
    "id": "p7",
    "n": 7,
    "title": "Proptech & Property Data",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS proptech and property-data\ncompany decision makers and deliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\nThis is my highest-value list — spend as much FREE exploration as you need to\nget the filter right, but still finish and still export.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: Software and data companies serving real estate — property data,\nvaluation/AVM, listings platforms, property management software, mortgage and\ntitle tech. 11-200 employees. COUNTRIES.\njob_level: founder, owner, president, c-suite. c-suite is correct here — CTO\nand Chief Product Officer are real buyers for engineering work.\n\nINDUSTRY — the hard part. \"Proptech\" is NOT a LinkedIn category; autocomplete\nmaps it back to real estate, which is wrong. Instead autocomplete\nlinkedin_category for \"software development\", then \"information technology &\nservices\", then \"data infrastructure and analytics\" (one per call) and use\nthose SOFTWARE categories. Fallback: naics_category 511210 or 518210.\nSoftware categories are huge, so INTENT does the narrowing here, not the\ncategory.\n\nINTENT: resolve via autocomplete and include — \"web scraping\", \"data\nextraction\", \"ai automation\", \"data enrichment\", \"alternative data\". If a\ntopic does not resolve, use the closest match autocomplete returns and say so\nat the end.\n\nVERIFY: the company description must mention PROPERTY, REAL ESTATE, HOUSING,\nMORTGAGE or VALUATION. Reject generic SaaS. If under half the sample\nqualifies, add property-adjacent intent or tighten the category, then\nre-sample. Because the reject rate is high, fetch 3x ROWS here, not 2x — the\nextra rows are free. If after several free attempts you still cannot get\npast half, export the best you have and flag which rows are property-related.\n\nEXCLUDE: none yet — this vertical has not been run. Report the dataset_id at\nthe end so I can add one here.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords (confirmed failure mode) or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> drop the least specific intent topic, then\n  widen size. Export what you have rather than returning empty.\n- Generic SaaS present     -> add property-specific intent, tighten category\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              several rounds expected here, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve every category and intent topic through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 3x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to proptech_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, how many\nrows have a non-empty company_description, and how many are genuinely\nproperty-related."
  },
  {
    "id": "p8",
    "n": 8,
    "title": "SaaS Founders",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS SaaS founders and CTOs and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nWHO: B2B SaaS companies, 11-200 employees. COUNTRIES.\njob_level: founder, owner, president, c-suite. c-suite is correct here — CTO\nis the buyer for engineering work.\n\nINDUSTRY: autocomplete linkedin_category for \"software development\", then\nseparately \"technology, information and internet\" — one concept per call.\n\nVERIFY: companies that SELL SOFTWARE. Reject IT consultancies, staffing\nagencies, resellers and anything describing itself as an agency or services\nfirm.\n\nINTENT: resolve via autocomplete and include — \"ai automation\", \"ai\nconsulting\", \"enterprise ai\", \"ai api services\". This vertical has the\nrichest intent data on my list, so keep intent ON and prefer higher scores.\n\nEXCLUDE: none yet — this vertical has not been run. Report the dataset_id at\nthe end so I can add one here.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords or company_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> drop the least specific intent topic, then\n  widen size. Export what you have rather than returning empty.\n- Consultancies present    -> tighten to software-product categories\n- Sample fails VERIFY      -> change filters and re-sample; this is free,\n                              repeat until it looks right, THEN export\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve any categories and intent topics through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to saas_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I care\n   about lands in its \"unrecognised\" list, tell me — the script needs a new\n   mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 5 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, and how\nmany rows have a non-empty company_description."
  },
  {
    "id": "p9",
    "n": 9,
    "title": "Accounting & Bookkeeping",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS accounting and bookkeeping firm owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\nThe 2026-09-10 run cost 600 credits because it exported a 150-row table with\nfour enrichments and never passed `limit`. Pass `limit`.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nTHIS PROMPT IS UNVERIFIED. Prompts 1-8 have been through real runs and their\nfilters are what those runs proved. This one has not: the categories and\ntitles below are a starting point, not a result. So step 2 of the RUN ORDER\nmatters more here than anywhere — read the statistics distribution and fix\nthe filter there, before anything is billable. Then tell me in the report\nexactly which filters you changed, so I can fold them back into this prompt\nand it stops being a guess.\n\nWHO: CPA firms, bookkeeping practices and tax preparers. 1-50 employees.\nCOUNTRIES. Small enough that the owner still touches the work — at 50+ there\nis an operations manager between the owner and the manual process, and the\npitch has to go through them.\n\nJOB TARGETING — UNVERIFIED, check the distribution at step 2.\njob_level: owner, founder, partner.\nThen ALSO filter job_title, resolved via autocomplete:\n  [\"managing partner\", \"founder\", \"owner\", \"principal\", \"cpa\",\n   \"certified public accountant\", \"accountant\", \"bookkeeper\",\n   \"tax preparer\", \"practice owner\"]\nExpected failure mode, by analogy with the real estate run: \"partner\" and\n\"principal\" are likely to pull in advisory, consulting and wealth-management\nfirms rather than practices that do the books. If the sample shows that,\ndrop \"principal\" first and keep the explicit CPA/bookkeeper titles.\n\nINDUSTRY — UNVERIFIED. Start with linkedin_category:\n  [\"accounting\", \"bookkeeping\", \"tax preparation\"]\nNOT \"financial services\" or \"investment management\" — the same trap as real\nestate, where the broad category returned capital-markets firms. NAICS 541211\n(offices of CPAs) and 541219 (other accounting services) are the fallback.\n\nVERIFY: firms that keep books and file returns for OTHER businesses. Reject\nwealth management, financial advisory, investment firms, and in-house\naccountants at non-accounting companies. The company_description is the\ntell — a real practice describes services for clients (\"bookkeeping, payroll,\ntax filing for small businesses\"), an advisory firm describes assets under\nmanagement. If more than a third of the sample is advisory, tighten TITLES\nand re-sample before enriching.\n\nINTENT: none. Small practices are unlikely to register enough research\nsignal, and the real estate run showed intent filters collapse a narrow\ncategory to single-digit results.\n\nEXCLUDE: no dataset yet — this vertical has never been run. After the first\nrun, paste the dataset_id here so the next one does not re-charge for the\nsame people.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords (it matches any site that merely mentions the service) or\ncompany_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> widen company_size first, then add the fallback\n  NAICS alongside the category. Export what you have rather than returning\n  empty.\n- Advisory/wealth firms   -> drop \"principal\" and \"partner\" from job_title,\n  keep \"cpa\", \"bookkeeper\", \"tax preparer\". Narrowing the category alone did\n  not work on the real estate run; expect the same here\n- Non-US countries empty   -> run them separately; the vocabulary differs\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve the categories and every job_title through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it, and on\n   this prompt do not trust the filters above without reading it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to accounting_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 6 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, how many\nrows have a non-empty company_description, and the country split.\nBecause this prompt is unverified, the filter changes are the part I most\nneed: quote the exact category and job_title lists that produced a clean\nsample, so the next run starts from them."
  },
  {
    "id": "p10",
    "n": 10,
    "title": "Insurance Agencies",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS independent insurance agency owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\nThe 2026-09-10 run cost 600 credits because it exported a 150-row table with\nfour enrichments and never passed `limit`. Pass `limit`.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nTHIS PROMPT IS UNVERIFIED. Prompts 1-8 have been through real runs and their\nfilters are what those runs proved. This one has not: the categories and\ntitles below are a starting point, not a result. So step 2 of the RUN ORDER\nmatters more here than anywhere — read the statistics distribution and fix\nthe filter there, before anything is billable. Then tell me in the report\nexactly which filters you changed, so I can fold them back into this prompt\nand it stops being a guess.\n\nWHO: Independent insurance agencies and brokerages. 1-100 employees.\nCOUNTRIES. Independent agencies rather than carriers: a carrier has an\ninternal IT department and a procurement process, an independent agency owner\ndecides on their own.\n\nJOB TARGETING — UNVERIFIED, check the distribution at step 2.\njob_level: owner, founder.\nNOT president, for the reason the real estate run found: in this data\n\"president\" skews to divisional presidents at large carriers, which is the\nwrong buyer entirely.\nThen ALSO filter job_title, resolved via autocomplete:\n  [\"agency owner\", \"owner\", \"agency principal\", \"insurance broker\",\n   \"insurance agent\", \"broker owner\", \"managing partner\",\n   \"independent agent\", \"producer\"]\n\"Producer\" is insurance vocabulary for a salesperson and may be too junior —\ncheck whether those rows are decision makers before keeping it.\n\nINDUSTRY — UNVERIFIED. Start with linkedin_category:\n  [\"insurance\", \"insurance agencies and brokerages\"]\nExpect the broad \"insurance\" to pull carriers and insurtech vendors. The\nnarrow agencies/brokerages value is the one to keep if it returns enough.\nNAICS 524210 (insurance agencies and brokerages) is the fallback and is\ntighter than the category.\n\nVERIFY: agencies that SELL policies to clients, not carriers that underwrite\nthem and not software vendors selling to agencies. The employee count is a\nuseful second filter — a 5000-person \"insurance\" company is a carrier.\nReject insurtech, claims-processing outsourcers and consultancies.\n\nINTENT: none on the first run. If the pool turns out large, intent is worth\ntesting here — insurance is software-heavy enough that agencies may register\nresearch signal, unlike dental or ecommerce where it returned 3 records.\nTest it as a second run, not the first.\n\nEXCLUDE: no dataset yet — this vertical has never been run. After the first\nrun, paste the dataset_id here so the next one does not re-charge for the\nsame people.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords (it matches any site that merely mentions the service) or\ncompany_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> widen company_size first, then add the fallback\n  NAICS alongside the category. Export what you have rather than returning\n  empty.\n- Carriers or insurtech   -> add a company_size ceiling, drop the broad\n  \"insurance\" category and keep agencies/brokerages, or fall back to NAICS\n  524210 which is tighter than either\n- Non-US countries empty   -> run them separately; the vocabulary differs\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve the categories and every job_title through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it, and on\n   this prompt do not trust the filters above without reading it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to insurance_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 6 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, how many\nrows have a non-empty company_description, and the country split.\nBecause this prompt is unverified, the filter changes are the part I most\nneed: quote the exact category and job_title lists that produced a clean\nsample, so the next run starts from them."
  },
  {
    "id": "p11",
    "n": 11,
    "title": "Logistics & Freight",
    "body": "ROWS = 25\nCOUNTRIES = US, Canada, UK, Australia\nBALANCE = 200\n\nUsing the Vibe Prospecting connector, find ROWS freight brokerage and 3PL owners and\ndeliver a CSV. Work autonomously start to finish.\n\nSTANDING AUTHORISATION — this is my explicit, up-front approval, given before\nany tool call: skip samples and auto-export. Enrich with email + firmographics\nand export to CSV without pausing. I have already seen and accepted the cost\nmodel below, so treat this paragraph as the confirmation the export step asks\nfor. On the contact-details question, my answer is EMAILS ONLY — do not ask it.\nDo not stop to show me a preview and wait. Fix problems yourself and tell me\nafterwards. A CSV without emails is a failed run.\n\nCOST MODEL — this is how the connector actually bills:\n  credits = (rows in the exported table) x (2 per enrichment + 2 for the fetch)\nTwo enrichments here, so ROWS x 5 — email costs 2/row, firmographics 1/row,\nand the fetch itself is billed at export too. Charged once at export;\nautocomplete, statistics and show-sample are free, and a fetched table you\nnever export costs nothing. Call estimate-cost before exporting to confirm the\nnumber rather than trusting this arithmetic.\nThe 2026-09-10 run cost 600 credits because it exported a 150-row table with\nfour enrichments and never passed `limit`. Pass `limit`.\n\nROW SCALING — do this, do not ask me about it:\n  affordable = floor((BALANCE - 5) / 5)\n  export_rows = min(ROWS, affordable)\nPass export_rows as the `limit` parameter on export-to-csv. That caps what\ngets billed, so a big fetched table costs nothing extra. If export_rows <\nROWS, deliver the CSV anyway and say so in the report. Never cancel a run\nover budget; a smaller CSV beats no CSV. If a call fails for insufficient\ncredits, halve `limit` and retry, down to 5 rows.\n\nTHIS PROMPT IS UNVERIFIED. Prompts 1-8 have been through real runs and their\nfilters are what those runs proved. This one has not: the categories and\ntitles below are a starting point, not a result. So step 2 of the RUN ORDER\nmatters more here than anywhere — read the statistics distribution and fix\nthe filter there, before anything is billable. Then tell me in the report\nexactly which filters you changed, so I can fold them back into this prompt\nand it stops being a guess.\n\nWHO: Freight brokerages, third-party logistics providers and dispatch\nservices. 5-200 employees. COUNTRIES. Note the floor is 5 rather than 1: a\none-person dispatch operation has no process worth automating, which is the\nopposite of the other verticals here.\n\nJOB TARGETING — UNVERIFIED, check the distribution at step 2.\njob_level: owner, founder, c_level.\nc_level is included here where the other prompts exclude it — in logistics\nthe operations decision often sits with a COO or VP Operations rather than\nthe owner. If the sample comes back as large-carrier executives, remove it.\nThen ALSO filter job_title, resolved via autocomplete:\n  [\"owner\", \"founder\", \"president\", \"freight broker\", \"broker owner\",\n   \"logistics manager\", \"operations manager\", \"dispatch manager\",\n   \"vp operations\", \"director of operations\"]\n\nINDUSTRY — UNVERIFIED. Start with linkedin_category:\n  [\"freight and package transportation\", \"truck transportation\",\n   \"transportation, logistics, supply chain and storage\"]\nThe third is broad and will pull warehousing and shipping lines; keep it only\nif the narrower two are thin. NAICS 488510 (freight transportation\narrangement) is the fallback and is the closest match to brokerages\nspecifically.\n\nVERIFY: companies that ARRANGE or MOVE freight for customers. Reject\nwarehousing-only operators, shipping lines, last-mile delivery gig platforms,\nand logistics software vendors. The company_description should mention\nbrokering, dispatching, carriers or loads. A \"supply chain\" consultancy is\nthe wrong buyer.\n\nINTENT: none. Untested in this vertical; do not spend a first run finding\nout.\n\nEXCLUDE: no dataset yet — this vertical has never been run. After the first\nrun, paste the dataset_id here so the next one does not re-charge for the\nsame people.\n\nSEARCH: entity_type prospects, max_per_company 1, has_contact_details value\n\"email\", has_website true. Filter by job_level NOT job_department. Never use\nwebsite_keywords (it matches any site that merely mentions the service) or\ncompany_tech_stack_tech.\n\nFIX YOURSELF, do not ask:\n- Fewer than ROWS results  -> widen company_size first, then add the fallback\n  NAICS alongside the category. Export what you have rather than returning\n  empty.\n- Warehousing/software    -> drop the broad supply-chain category, keep\n  freight/truck transportation, or fall back to NAICS 488510\n- Large-carrier execs     -> remove c_level from job_level and keep owner\n  and founder only\n- Non-US countries empty   -> run them separately; the vocabulary differs\n- Tool error               -> retry once, then work around it\n\nRUN ORDER:\n1. Resolve the categories and every job_title through autocomplete first.\n2. fetch-entities-statistics with the SAME filters. Free. Read the pool\n   size and the distribution before pulling any rows:\n   - pool under ~3x ROWS  -> too narrow; widen per FIX YOURSELF and re-check\n   - pool over ~5000      -> too broad; the sample will be noise. Tighten\n                             the category or add the next filter down\n   - wrong shape          -> if the industry or job breakdown is obviously\n                             off-target, fix the filter HERE, before fetching\n   This is the cheapest place to catch a bad filter. Do not skip it, and on\n   this prompt do not trust the filters above without reading it.\n3. fetch-entities for about 2x ROWS. Free — a larger table costs nothing\n   because `limit` controls what is billed.\n4. show-sample and check against VERIFY. Re-filter and re-sample as needed.\n5. enrich-prospects, enrichments [\"enrich-prospects-contacts\"],\n   contact_types [\"email\"]. No phone numbers.\n6. enrich-business, enrichments [\"enrich-business-firmographics\"], on the\n   table returned by step 4. Without this the CSV has no company description,\n   industry or employee count and the CRM flags every row.\n   Use the NEW table_name each step returns — enrichments do not accumulate\n   on the original fetch table, and exporting the wrong one loses what you\n   paid for.\n7. estimate-cost on the final table to confirm before spending.\n8. export-to-csv on that final table with limit = export_rows.\n\n9. THEN convert locally, which is free:\n\n     python C:/Users/Proxi/Documents/Claude/outreach-crm/backend/scripts/csv_to_standard.py <the-export>.csv\n\n   That writes <the-export>-standard.csv in my CRM's column names. Rename it\n   to logistics_<YYYYMMDD-HHMM>.csv and give me the full path. If a column I\n   care about lands in its \"unrecognised\" list, tell me — the script needs a\n   new mapping, do not hand-edit the CSV.\n\n   `email` is required on every row. Empty cells are fine EXCEPT\n   company_description / industry / employee_range being empty on EVERY row,\n   which means step 6 did not land. Some individual rows legitimately have a\n   blank industry; that is missing source data, not a failure.\n\nREPORT, in this order: local file path, rows delivered vs ROWS requested,\ncredits spent, remaining balance, and the new dataset_id on its own line\nlabelled \"paste into EXCLUDE\" — I add it to this prompt so the next run does\nnot re-charge me for the same people. Then any filter you changed, how many\nrows have a non-empty company_description, and the country split.\nBecause this prompt is unverified, the filter changes are the part I most\nneed: quote the exact category and job_title lists that produced a clean\nsample, so the next run starts from them."
  }
];
