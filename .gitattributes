# Normalize to LF in the repo so the Linux containers get correct line endings
# regardless of the checkout platform.
* text=auto eol=lf

# entrypoint.sh runs inside the container; CRLF here breaks the shebang.
*.sh text eol=lf

*.png binary
*.jpg binary
*.ico binary
